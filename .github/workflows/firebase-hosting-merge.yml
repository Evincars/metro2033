# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Forcing Node 20 bypasses the recent HTTP keep-alive bug
      - run: npm install --legacy-peer-deps && echo '-----Install Done 👌-----' && npm run build && echo '-----Build Done 👌-----'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_METRO_UNIVERSE }}
          channelId: live
          projectId: metro-universe
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
